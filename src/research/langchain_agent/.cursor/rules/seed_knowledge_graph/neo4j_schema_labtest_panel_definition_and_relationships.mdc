---
alwaysApply: false
---

## Vector Search / Embedding Convention

For vector search and Neo4j vector indexes, nodes and relationships support:

- **searchText** (string): The text used to create embeddings and perform vector searches. Provide explicitly, or derive from searchFields.
- **searchFields** (string[]): Optional. Property names whose values concatenate to form searchText. Use either custom searchText OR searchFields.
- **embedding** (list<float>): The embedding vector stored on the node/relationship.
- **embeddingModel** (string): Model used to generate embeddings (e.g., text-embedding-3-small).
- **embeddingDimensions** (int): Dimension count of the embedding vector.

---

PanelDefinition
Node

Label: :PanelDefinition

Primary identifier

panelDefinitionId (string UUID)

Properties

panelDefinitionId (uuid string)

createdAt (datetime)

canonicalName (string)

aliases (string[])

description (string)

validAt (datetime)

invalidAt (datetime)

expiredAt (datetime)

Vector search
searchText (string) — custom text, or derived from searchFields
searchFields (string[]) — optional: ["canonicalName","aliases","description"]
embedding (list<float>)
embeddingModel (string)
embeddingDimensions (int)

Relationships (outgoing)

1. (pd:PanelDefinition)-[r:INCLUDES_LABTEST]->(lt:LabTest)
   Relationship properties

required (boolean)

quantity (number)

notes (string)

claimIds (string[])

createdAt, validAt, invalidAt, expiredAt (datetime)

Vector search
searchText (string) — custom text, or derived from searchFields
searchFields (string[]) — optional: ["notes"]
embedding (list<float>)
embeddingModel (string)
embeddingDimensions (int)

Target node: :LabTest

labTestId (uuid string)

createdAt (datetime)

name (string)

synonyms (string[])

loincCodes (string[])

cptCodes (string[])

whatItMeasures (string)

prepRequirements (string)

validAt, invalidAt, expiredAt (datetime)

2. (pd:PanelDefinition)-[r:INCLUDES_BIOMARKER]->(bm:Biomarker)
   Relationship properties

claimIds (string[])

createdAt, validAt, invalidAt, expiredAt (datetime)

Vector search
searchText (string) — custom text, or derived from searchFields
searchFields (string[]) — optional: (relationship type + target node context)
embedding (list<float>)
embeddingModel (string)
embeddingDimensions (int)

Target node: :Biomarker

biomarkerId (uuid string)

createdAt (datetime)

name (string)

synonyms (string[])

description (string)

clinicalDomains (ClinicalDomain[])
ENUM: ClinicalDomain
Values: CARDIOVASCULAR | METABOLIC | ENDOCRINE | HORMONAL | IMMUNOLOGICAL | HEMATOLOGY | ONCOLOGICAL | RENAL | HEPATIC | GASTROINTESTINAL | RESPIRATORY | NEUROLOGICAL | COGNITIVE | PSYCHIATRIC | MUSCULOSKELETAL | DERMATOLOGICAL | REPRODUCTIVE | UROLOGICAL | OPHTHALMOLOGICAL | OTOLARYNGOLOGY | INFECTIOUS_DISEASE | TOXICOLOGY | GENETICS | NUTRITIONAL | INFLAMMATION | LONGEVITY | OTHER | UNKNOWN

unitsCommon (Unit[])
ENUM: Unit
Values: MG_PER_DL | G_PER_DL | MMOL_PER_L | UMOL_PER_L | NMOL_PER_L | PMOL_PER_L | MEQ_PER_L | IU_PER_L | U_PER_L | G | MG | UG | NG | MOL | MMOL | UMOL | NMOL | L | DL | ML | UL | CELLS_PER_UL | CELLS_PER_L | THOUSAND_PER_UL | MILLION_PER_UL | PERCENT | RATIO | INDEX | SCORE | UNITLESS | MMHG | KPA | SEC | MIN | HOUR | DAY | WEEK | C | CM | INCH | U_PER_G_HB | IU_PER_ML | OTHER | UNKNOWN

interpretationNotes (string)

validAt, invalidAt, expiredAt (datetime)

Vector search (Biomarker)
searchText (string) — custom text, or derived from searchFields
searchFields (string[]) — optional: ["name","synonyms","description"]
embedding (list<float>)
embeddingModel (string)
embeddingDimensions (int)

LabTest
Node

Label: :LabTest

Primary identifier

labTestId (string UUID)

Properties

labTestId (uuid string)

createdAt (datetime)

name (string)

synonyms (string[])

loincCodes (string[])

cptCodes (string[])

whatItMeasures (string)

prepRequirements (string)

validAt (datetime)

invalidAt (datetime)

expiredAt (datetime)

Vector search
searchText (string) — custom text, or derived from searchFields
searchFields (string[]) — optional: ["name","synonyms","whatItMeasures","prepRequirements"]
embedding (list<float>)
embeddingModel (string)
embeddingDimensions (int)

Relationships (outgoing)

1. (lt:LabTest)-[r:MEASURES]->(bm:Biomarker)
   Relationship properties

role (BiomarkerRole) (ENUM already exists in your enums)

claimIds (string[])

createdAt, validAt, invalidAt, expiredAt (datetime)

Target node: :Biomarker (same as above)

Vector search (MEASURES)
searchText (string) — custom text, or derived from searchFields
searchFields (string[]) — optional: ["role"]
embedding (list<float>)
embeddingModel (string)
embeddingDimensions (int)

2. (lt:LabTest)-[r:USES_METHOD]->(mm:MeasurementMethod)
   Relationship properties

methodRole (MethodRole) (ENUM already exists)

claimIds (string[])

createdAt, validAt, invalidAt, expiredAt (datetime)

Target node: :MeasurementMethod

methodId (uuid string)

createdAt (datetime)

canonicalName (string)

methodFamily (MethodFamily)
ENUM: MethodFamily
Values: IMMUNOASSAY | CHEMISTRY | HEMATOLOGY | MOLECULAR | GENOMICS | PROTEOMICS | METABOLOMICS | LIPIDOMICS | MICROBIOLOGY | COAGULATION | TOXICOLOGY | URINALYSIS | FLOW_CYTOMETRY | MASS_SPECTROMETRY | CHROMATOGRAPHY | ELECTROPHORESIS | BIOSENSOR | IMAGING | POINT_OF_CARE | OTHER | UNKNOWN

analyticPrinciple (AnalyticPrinciple)
ENUM: AnalyticPrinciple
Values: ELISA | CLIA | ECLIA | RIA | NEPHELOMETRY | TURBIDIMETRY | ENZYMATIC | COLORIMETRIC | SPECTROPHOTOMETRY | FLUOROMETRY | CHEMILUMINESCENCE | ELECTROCHEMISTRY | PCR | QPCR | RT_PCR | DIGITAL_PCR | ISOTHERMAL_AMPLIFICATION | NGS | SANGER | LC_MS | GC_MS | HPLC | ION_CHROMATOGRAPHY | CAPILLARY_ELECTROPHORESIS | GEL_ELECTROPHORESIS | FLOW_CYTOMETRY | MICROSCOPY | LATERAL_FLOW | BIOSENSOR_ELECTRODE | OPTICAL_BIOSENSOR | CULTURE | MALDI_TOF | OTHER | UNKNOWN

typicalCvPercentMin (number)

typicalCvPercentMax (number)

validAt, invalidAt, expiredAt (datetime)

Vector search (MeasurementMethod)
searchText (string) — custom text, or derived from searchFields
searchFields (string[]) — optional: ["canonicalName","methodFamily","analyticPrinciple"]
embedding (list<float>)
embeddingModel (string)
embeddingDimensions (int)

Vector search (USES_METHOD)
searchText (string) — custom text, or derived from searchFields
searchFields (string[]) — optional: ["methodRole"]
embedding (list<float>)
embeddingModel (string)
embeddingDimensions (int)

3. (lt:LabTest)-[r:REQUIRES_SPECIMEN]->(s:Specimen)
   Relationship properties (enum insertions only)

specimenRole (SpecimenRole) (ENUM exists)

collectionSetting (CollectionSetting) (ENUM exists)

collectionMethod (CollectionMethod) (ENUM exists)

collectionTimeWindow (CollectionTimeWindow) (ENUM exists)

processingAdditive (ProcessingAdditive) (ENUM exists)

sex (Sex) (ENUM exists)

measurementState (MeasurementState) (ENUM exists)

timeOfDay (TimeOfDay) (ENUM exists)

appliesWhen (AppliesWhen) (ENUM exists)

thresholdDirection (ThresholdDirection) (ENUM exists)

thresholdUnit (Unit)
ENUM: Unit
Values: (same Unit list as above)

(All other fields unchanged: fastingRequired/hours, processing/stability numeric fields, tags, notes, provenance, etc.)

Target node: :Specimen

specimenId (uuid string)

createdAt (datetime)

canonicalName (string)

specimenType (SpecimenType)
ENUM: SpecimenType
Values: WHOLE_BLOOD | PLASMA | SERUM | DBS | BUCCAL_SWAB | SALIVA | URINE | STOOL | SPUTUM | BREATH_CONDENSATE | CSF | SEMEN | VAGINAL_SWAB | NASAL_SWAB | TISSUE_BIOPSY | HAIR | NAIL | SWEAT | OTHER | UNKNOWN

matrix (SpecimenMatrix)
ENUM: SpecimenMatrix
Values: LIQUID | SOLID | SEMI_SOLID | FILTER_PAPER | SWAB | BREATH | OTHER | UNKNOWN

biologicalDomain (BiologicalDomain)
ENUM: BiologicalDomain
Values: BLOOD | URINARY | GASTROINTESTINAL | RESPIRATORY | NERVOUS_SYSTEM | REPRODUCTIVE | MUSCULOSKELETAL | INTEGUMENTARY | IMMUNE | ENDOCRINE | HEPATIC | RENAL | CARDIOVASCULAR | MICROBIOME | OTHER | UNKNOWN

collectionContextCategory (CollectionContextCategory)
ENUM: CollectionContextCategory
Values: ROUTINE_SCREENING | DIAGNOSTIC_WORKUP | BASELINE | FOLLOW_UP | ACUTE_SYMPTOMS | CHRONIC_MANAGEMENT | PRE_PROCEDURE | POST_PROCEDURE | PRE_INTERVENTION | POST_INTERVENTION | ATHLETIC_PERFORMANCE | LONGEVITY_OPTIMIZATION | RESEARCH_STUDY | OTHER | UNKNOWN

validAt, invalidAt, expiredAt (datetime)

Vector search (Specimen)
searchText (string) — custom text, or derived from searchFields
searchFields (string[]) — optional: ["canonicalName","specimenType","matrix","biologicalDomain"]
embedding (list<float>)
embeddingModel (string)
embeddingDimensions (int)

Vector search (REQUIRES_SPECIMEN)
searchText (string) — custom text, or derived from searchFields (e.g. specimenRole, collectionSetting, collectionMethod)
searchFields (string[]) — optional: ["specimenRole","collectionSetting","collectionMethod","notes"]
embedding (list<float>)
embeddingModel (string)
embeddingDimensions (int)

4. (lt:LabTest)-[r:USES_PLATFORM]->(tp:TechnologyPlatform)

(unchanged; PlatformType already exists in your enums)

Vector search (TechnologyPlatform)
searchText (string) — custom text, or derived from searchFields
searchFields (string[]) — optional: ["canonicalName","aliases","platformType","description"]
embedding (list<float>)
embeddingModel (string)
embeddingDimensions (int)

Vector search (USES_PLATFORM)
searchText (string) — custom text, or derived from searchFields
searchFields (string[]) — optional: (relationship type + target platform context)
embedding (list<float>)
embeddingModel (string)
embeddingDimensions (int)

Quick edge-name index (copy/paste)
PanelDefinition -[:INCLUDES_LABTEST]-> LabTest
PanelDefinition -[:INCLUDES_BIOMARKER]-> Biomarker
LabTest -[:MEASURES]-> Biomarker
LabTest -[:USES_METHOD]-> MeasurementMethod
LabTest -[:REQUIRES_SPECIMEN]-> Specimen
LabTest -[:USES_PLATFORM]-> TechnologyPlatform
